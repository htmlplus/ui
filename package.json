{
  "name": "htmlplus",
  "version": "0.0.0",
  "scripts": {
    "clean": "lerna run clean",
    "clean:hard": "npm run clean && lerna clean",
    "init": "lerna bootstrap",
    "publish:latest": "lerna publish from-package",
    "publish:next": "lerna publish from-package --dist-tag next",
    "publish:release": "lerna version --no-private --preid next",
    "prepublishOnly": "npm run preversion",
    "preversion": "lerna run clean && lerna run bootstrap && lerna run build --scope @htmlplus/react --stream",
    "start": "lerna run start --scope @htmlplus/core --stream"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}