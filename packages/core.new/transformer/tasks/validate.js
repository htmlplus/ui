import * as CONSTANTS from '../constants.js';

export const validate = (context) => {

    const { ast } = context;

    // const component = ast
    //     .getClasses()
    //     .find((item) => item.getDecorator(CONSTANTS.TOKEN_DECORATOR_COMPONENT));

    // if (!component)
    //     throw new Error('Invalid .tsx file');
}