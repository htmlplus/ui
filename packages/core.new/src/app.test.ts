// const timeout = 15000;
// beforeAll(async () => {await page.goto('https://www.google.com/', {waitUntil: 'domcontentloaded'});

// });
// describe('Test page title and header', () => {
//   test('page title', async () => {
//     const title = await page.title(); expect(title).toBe('Google'); }, timeout);

// });


